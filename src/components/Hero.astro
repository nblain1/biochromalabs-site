---
import { Image } from 'astro:assets';
import { siteData } from '@data/siteData';
import HeroImage from "@assets/lab-hero.svg"
---

<section class="relative min-h-screen flex items-center justify-center bg-linear-to-br from-slate-900 via-slate-800 to-slate-900 pt-20 overflow-hidden">
  <!-- Background Pattern -->
  <div class="absolute inset-0 opacity-10">
    <div class="absolute inset-0" style="background-image: radial-gradient(circle at 2px 2px, white 1px, transparent 0); background-size: 40px 40px;"></div>
  </div>

  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
      <!-- Text Content -->
      <div class="text-center lg:text-left">
        <h1 class="text-5xl md:text-6xl lg:text-7xl font-bold text-white mb-6 leading-tight">
          {siteData.name}
        </h1>
        <p class="text-xl md:text-2xl text-gray-300 mb-8 leading-relaxed">
          {siteData.tagline}
        </p>
        <div class="flex flex-col sm:flex-row gap-4 sm:gap-6 justify-center lg:justify-start mb-12">
          <a
            href="/contact"
            class="bg-[#4db848] hover:bg-[#3ea942] text-white px-8 py-4 rounded-lg font-bold text-lg transition-all transform inline-flex items-center justify-center gap-3"
          >
            Request a Quote
            <i class="ri-arrow-right-line"></i>
          </a>
          <a
            href="/services"
            class="bg-transparent border-2 border-white text-white hover:bg-white hover:text-slate-900 px-8 py-4 rounded-lg font-bold text-lg transition-all inline-flex items-center justify-center gap-3"
          >
            View Services
            <i class="ri-flask-line"></i>
          </a>
        </div>

        <ul class="space-y-3 text-left max-w-lg mx-auto lg:mx-0">
          <li class="flex items-start gap-3 text-gray-300">
            <i class="ri-check-line text-[#4db848] mt-1"></i>
            Quality systems with documented methods and review checkpoints.
          </li>
          <li class="flex items-start gap-3 text-gray-300">
            <i class="ri-check-line text-[#4db848] mt-1"></i>
            Experienced analysts across chemistry and microbiology workflows.
          </li>
          <li class="flex items-start gap-3 text-gray-300">
            <i class="ri-check-line text-[#4db848] mt-1"></i>
            Clear, audit-ready reporting with chain-of-custody handling.
          </li>
        </ul>
      </div>

      <!-- Hero Image -->
      <div class="hidden lg:block">
        <div class="relative">
          <div class="aspect-square rounded-2xl overflow-hidden bg-slate-800 border-2 border-slate-700 shadow-2xl">
            <Image
              src={HeroImage}
              alt="Laboratory analysis"
              width={800}
              height={800}
              class="w-full h-full object-cover"
              loading="eager"
            />
          </div>
          <div class="absolute -bottom-6 -left-6 bg-[#264177] p-6 rounded-xl shadow-xl">
            <div class="flex items-center gap-3">
              <div class="w-12 h-12 bg-white rounded-lg flex items-center justify-center">
                <i class="ri-shield-check-line text-2xl text-[#4db848]"></i>
              </div>
              <div>
                <div class="text-white font-bold text-lg">Quality-first</div>
                <div class="text-white/80 text-sm">Documented methods</div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Scroll Indicator -->
  <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce hidden lg:block">
    <i class="ri-arrow-down-line text-2xl text-white"></i>
  </div>
</section>
