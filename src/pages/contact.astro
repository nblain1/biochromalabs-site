---
import Layout from '../layouts/Layout.astro';
import { siteData, departmentEmails } from '../data/siteData';
---

<Layout title={`Contact - ${siteData.name}`} description="Direct access to BioChroma Analytical Labs for project scoping, sample submission, and technical support.">
  <section class="home-hero">
    <div class="home-shell">
      <nav class="breadcrumb" aria-label="Breadcrumb">
        <a href="/">Home</a>
        <span aria-hidden="true">&gt;</span>
        <span class="text-emerald-400 opacity-80">Contact</span>
      </nav>

      <div class="hero-grid mb-16">
        <div class="hero-copy">
          <p class="hero-eyebrow">Contact & Support</p>
          <h1>Direct access to our scientific team.</h1>
          <p class="hero-lead">
            Whether you are initiating a new stability study or need to schedule a courier pickup, use the direct channels below to reach the appropriate department.
          </p>
        </div>

        <aside class="hero-media-frame" aria-label="Client Support">
          <img
            src="https://images.unsplash.com/photo-1573164713988-8665fc963095?w=800&q=80" 
            alt="BioChroma Client Consultation"
            loading="eager"
          />
          <div class="hero-media-caption">
            <p>Technical Support</p>
            <span>Direct access to lead scientists.</span>
          </div>
        </aside>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-16">
        <a href={`mailto:${siteData.email}`} class="group block bg-white/5 border border-white/10 p-8 rounded-xl transition-all duration-300 hover:-translate-y-1 hover:border-emerald-400/30 hover:bg-emerald-400/5 hover:shadow-[0_10px_30px_-10px_rgba(36,199,143,0.15)]">
          <div class="w-14 h-14 rounded-xl bg-emerald-500/10 text-emerald-400 flex items-center justify-center text-2xl mb-6">
            <i class="ri-flask-line"></i>
          </div>
          <div class="action-content">
            <h3 class="text-xl font-bold text-white mb-2">New Project Scoping</h3>
            <p class="text-slate-300 text-sm mb-4">Email our technical leads for quotes and testing plans.</p>
            <span class="flex items-center gap-2 text-emerald-400 font-semibold text-sm">
              {siteData.email} <i class="ri-arrow-right-line group-hover:translate-x-1 transition-transform"></i>
            </span>
          </div>
        </a>

        <a href="mailto:sales@biochromalabs.com" class="group block bg-white/5 border border-white/10 p-8 rounded-xl transition-all duration-300 hover:-translate-y-1 hover:border-emerald-400/30 hover:bg-emerald-400/5 hover:shadow-[0_10px_30px_-10px_rgba(36,199,143,0.15)]">
          <div class="w-14 h-14 rounded-xl bg-emerald-500/10 text-emerald-400 flex items-center justify-center text-2xl mb-6">
            <i class="ri-mail-open-line"></i>
          </div>
          <div class="action-content">
            <h3 class="text-xl font-bold text-white mb-2">Sales & Quotes</h3>
            <p class="text-slate-300 text-sm mb-4">Send your scope, sample matrix, and timing needs for a tailored testing plan.</p>
            <span class="flex items-center gap-2 text-emerald-400 font-semibold text-sm">
              sales@biochromalabs.com <i class="ri-arrow-right-line group-hover:translate-x-1 transition-transform"></i>
            </span>
          </div>
        </a>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-3 gap-12 items-start">
        
        <div class="lg:col-span-2">
          <h2 class="flex items-center gap-3 text-2xl font-bold text-white mb-8">
            <i class="ri-node-tree text-emerald-400"></i> Department Directory
          </h2>
          <div class="flex flex-col gap-4">
            {departmentEmails.map((dept) => (
              <a href={`mailto:${dept.email}`} class="group flex flex-col md:flex-row md:items-center justify-between p-6 bg-white/5 border border-white/10 rounded-xl hover:border-emerald-400/30 hover:bg-emerald-500/5 transition-all duration-300">
                <div>
                  <span class="block text-white font-semibold text-lg mb-1">{dept.department}</span>
                  <span class="text-emerald-400 text-sm">{dept.email}</span>
                </div>
                <div class="flex items-center gap-4 mt-4 md:mt-0">
                  <p class="text-slate-400 text-sm m-0">{dept.description}</p>
                  <i class="ri-mail-send-line text-xl text-white/20 group-hover:text-emerald-400 transition-colors"></i>
                </div>
              </a>
            ))}
          </div>
        </div>

        <aside class="lg:col-span-1 flex flex-col gap-6">
          <div class="bg-white/5 border border-white/10 p-8 rounded-xl">
            <h3 class="text-white mb-6 text-lg uppercase tracking-wider font-bold">Lab Headquarters</h3>
            
            <div class="flex gap-4 mb-6">
              <i class="ri-map-pin-2-line text-emerald-400 text-xl mt-1"></i>
              <p class="text-slate-300 text-sm leading-relaxed">
                1309 Record Crossing Road<br/>Dallas, TX 75235
              </p>
            </div>
            
            <div class="flex gap-4 mb-6">
              <i class="ri-phone-line text-emerald-400 text-xl"></i>
              <a href={`tel:${siteData.phone}`} class="text-slate-300 text-sm hover:text-emerald-400 transition-colors">
                {siteData.phone}
              </a>
            </div>
            
            <div class="flex gap-4">
              <i class="ri-time-line text-emerald-400 text-xl"></i>
              <p class="text-slate-300 text-sm">Mon - Fri: 8:30 AM - 5:30 PM CT</p>
            </div>
          </div>
          
          <div class="flex gap-4 p-6 border-l-2 border-emerald-400 bg-emerald-500/5">
            <i class="ri-flashlight-line text-emerald-400 text-xl"></i>
            <p class="text-sm text-slate-400 leading-relaxed">
              Technical inquiries are reviewed by staff scientists and typically receive a response within 24 business hours.
            </p>
          </div>
        </aside>
        
      </div>
    </div>
  </section>
</Layout>